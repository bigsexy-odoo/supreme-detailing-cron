# NEVER commit credentials — they live in GitHub repo secrets, not the repo.
.env
.env.*
*.key

# Python
__pycache__/
*.pyc

# Local run artefacts
logs/
*.log
.chat_bridge_state.json
.*_state.json

# Single-instance lock (runtime)
.sync_bookings.lock

# Session diagnostic probes
_reaper_*.py
_reaper_*.txt

gbp_client_secret.json
_ct_id.txt
backups/_*
